import requests
import json

# calories_api = requests.get("https://developer.edamam.com/edamam-nutrition-api-demo", "200g chicken")

# data = calories_api.text
# # file_name = 'first_food.json'
# # with open(file_name, 'w') as file:
# #     json.dump(data, file, indent = 2)
